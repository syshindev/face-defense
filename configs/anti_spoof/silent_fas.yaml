# Silent-Face-Anti-Spoofing model configuration
model:
  name: "silent_fas"
  scales: [2.7, 4.0]
  input_size: [80, 80]
  threshold: 0.5

weights:
  model_dir: "${project_root}/weights/silent_fas"

inference:
  batch_size: 1
  device: "cuda:0"
